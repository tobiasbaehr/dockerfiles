containers:
  - name: phpmyadmin
    image: reinblau/phpmyadmin
    dockerfile: .
    run:
      publish-all: true
      detach: true
      hostname: "phpmyadmin.dev"
      link: ["mysql:db"]
      env: ["VIRTUAL_HOST=phpmyadmin.dev"]
