containers:
  - name: basic_lamp
    image: tobiasb/basic_lamp
    dockerfile: .
    run:
      publish-all: true
      detach: true
      hostname: "basic_lamp.dev"
      link: ["mysql:db"]
      env: ["VIRTUAL_HOST=basic_lamp.dev"]
